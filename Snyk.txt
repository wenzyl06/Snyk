from flask import Flask, request

app = Flask(__name__)

@app.route('/')
def index():
    name = request.args.get('name', '')
    # Vulnerable to reflected XSS!
    return f'<h1>Hello, {name}!</h1><form>Enter your name: <input name="name"></form>'

if __name__ == '__main__':
    app.run(debug=True)
